# Research Task: Layout display in Waybar

Research if Hyprland can broadcast the current 'general:layout' (master/dwindle) to the taskbar (specifically Waybar). Check:
1. Is there a built-in Hyprland Waybar module for layout display?
2. If not, what is the standard method (e.g., polling `hyprctl`, or `socat` for IPC events) to update a custom Waybar label module with the layout name?
Provide a brief summary of the best approach.

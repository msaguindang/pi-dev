# All .pi/ runtime state — machine-specific, never commit
.pi/
.worktrees/
.taskplane-tasks/
taskplane.json
auth.json
sessions/
plan_output.txt
run-history.jsonl

# Pi runtime artifacts (machine-specific)
pi-crash.log
intercom/
npm/
scout-scanner.sh

# Local runtime state, database, and telemetry logs
cost-history.jsonl
cost-sync-status.json
logs/
usage-data/

# Standalone utility scripts (not part of any extension)
scripts/

# Claude Code local permission overrides (machine-specific)
.claude/settings.local.json

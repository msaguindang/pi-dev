# Taskplane runtime artifacts (machine-specific, do not commit)
.pi/batch-state.json
.pi/batch-history.json
.pi/lane-state-*
.pi/merge-result-*
.pi/merge-request-*
.pi/worker-conversation-*
.pi/orch-logs/
.pi/orch-abort-signal
.pi/settings.json
.worktrees/
.taskplane-tasks/
taskplane.json

# Pi project-local packages (if using pi install -l)
.pi/npm/
auth.json

# Pi runtime artifacts (machine-specific, do not commit)
.pi/diagnostics/
.pi/mailbox/
.pi/runtime/
.pi/supervisor/
.pi/telemetry/
sessions/
plan_output.txt
run-history.jsonl

# Pi runtime artifacts (machine-specific)
pi-crash.log
intercom/
npm/
scout-scanner.sh

# Local runtime state, database, and telemetry logs
cost-history.jsonl
cost-sync-status.json
logs/
usage-data/

# Standalone utility scripts (not part of any extension)
scripts/
